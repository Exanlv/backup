Tool for automatic backups

Dependencies:
- Python
- arp-scan (make sure sudo can be used without password)
- sshfs
- tar
- gpg
- notify-send (on the machine being backed up)
- scp

Python dependencies:
`pip3 install -r ./pip`